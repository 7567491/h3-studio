# H3 Studio Frontend — example env file (Vite)
# Copy to .env.local and fill in real values. .env.local is git-ignored.

# === Optional: Sibling viewer gallery link (shows "Full gallery ↗" in History tab) ===
# Leave empty to hide the link entirely.
# Example: VITE_GALLERY_URL=https://gallery.example.com

VITE_GALLERY_URL=